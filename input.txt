class Main {
    int x;

    int[] bar(int a) {
        return new int[10];
    }

	int foo() {
        int y;
        //y = (this.bar(1+1))[0];
        x = (this.bar(1+1))[0];

        return x;
	}
}
