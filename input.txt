import io;

class VarargsAndArrayInit {
    int foo(int[] ar, int... a) {
        return ar.length + a.length;
    }

    int bar() {
        int res;
        int[] t;
        t = new int[3];

        res = this.foo((([t[2], 2, 3])), 4, 5, 6, 7, 8);

        return res;
    }
}
