class VarargsAndArrayInit {

    //int[] p;

    int foo(int[] ar, int... a) {
        return ar.length + a.length;
    }

    int bar() {
        int res;
        //int[] t;
        //t = new int[3];
        //p = new int[3];

        res = this.foo([(5+5), 2, 3], 4, 5, 6, 7, 8);

        return a;
    }
}